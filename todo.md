# TODO — 디자인 시스템 배선

2026-07-16 분석 결과(docs 개정 기준 감사: 토큰·컴포넌트·강제 장치·워크벤치 4축 점검): `apps/frontend/docs/design-system.md` 신설과 Storybook 고정(code-quality.md)으로 규칙은 완비됐으나 배선이 뒤따르지 않았다. 공백은 다섯이다: (1) 토큰 정의가 앱 `globals.css`에 있어 `packages/ui`가 앱이 정의해 줄 토큰에 기대는 보이지 않는 계약 상태다(문서 규칙: 디자인시스템 패키지가 소유하고 앱이 import). (2) 타이포 롤·모션·레이어 토큰이 없어 전 컴포넌트가 `text-sm`·`duration-150` 등 프레임워크 기본 스케일을 개별 조합한다(롤 재조립 금지 규칙 위반 상태). (3) "ESLint가 원시 색·치수 값을 막는다"는 문서 선언과 달리 해당 린트 규칙이 없고, `packages/ui`는 lint 스크립트 자체가 없다. (4) 버튼 시각의 링크 중복이 실증됐다 — PostList 빈 상태 CTA가 Button primary 클래스를 문자열로 복제하고 페이지네이션 링크가 secondary 유사 스타일을 중복하는데 Button은 `<button>` 전용이라 링크가 쓸 수 없다. (5) 스토리가 0개다 — 워크벤치·게이트(스모크·a11y) 미배선. 항목별 상세 작업 요청 프롬프트는 [`todo-prompt.md`](./todo-prompt.md)에 있다.

사용법: 새 세션에서 항목 하나를 골라 `todo-prompt.md`의 해당 프롬프트를 그대로 붙여넣는다. 각 프롬프트는 완료 시 이 파일의 해당 항목을 체크([x])하는 단계까지 포함한다.

아래 목록 순서가 곧 권장 실행 순서다. 1(토큰 소유권)이 맨 앞인 이유는 5(Storybook)의 구조적 전제라서다 — packages→apps 의존은 금지 방향이라 토큰이 앱에 있으면 워크벤치가 스타일 없이 렌더된다. 2(토큰 확장)는 1이 만든 정의처 위에 얹는다. 3(린트)은 2 이후여야 치환이 끝난 코드에서 게이트가 그린으로 출발한다(순서가 반대면 기존 코드 전체가 위반으로 시작). 4(컴포넌트 승격)는 2의 롤 토큰으로 승격분을 빚는다. 5는 1이 필수 전제이고 4 이후면 승격분까지 스토리 커버가 한 번에 끝난다. 2·3·4는 엄밀히는 부분 독립이나 위 순서가 재작업을 최소화한다.

공통 완료 기준(모든 항목): 루트 `pnpm verify`(turbo verify + depcruise + drift) 통과, 코드 생성 후 `apps/frontend/docs/architecture.md` 빌드 불변식 + `design-system.md` 완료 정의로 자기검증, 요청 범위 밖 기능 추가 금지. 이 단계는 배선이지 리디자인이 아니다 — 명시된 변경 외 화면의 시각 결과를 보존한다. 구현이 `apps/frontend/docs/`의 선언과 충돌하면 그 문서의 갱신을 같은 작업에 포함한다(편집 규약 준수). 문서는 범용 규칙이므로 프로젝트 고유 값·도메인 어휘를 넣지 않는다.

의도적 제외(이번 단계에서 다루지 않기로 판단한 것):

- 시각 회귀 검사 도구(Chromatic·Playwright 스크린샷 등): design-system.md·code-quality.md가 선제 도입을 금지한다 — 5번 정착 후 도입 시점에 code-quality.md가 도구를 고정한다.
- 아이콘 세트 결정: 현재 아이콘 사용처가 0이다(화살표는 텍스트 문자 `→`). 첫 아이콘 수요 때 단일 세트 규칙을 적용한다.
- cva 도입: 현행 `Record<Variant, string>` 패턴으로 충분하다 — variant 축이 둘 이상(variant × size)이 될 때 재평가.
- 공유 `packages/eslint-config`·`packages/tsconfig` 승격: 둘째 앱 추가 시(code-quality.md 규칙 유지). 3번은 현행 배치 원칙 안에서 배선한다.
- `packages/entities`·`packages/auth` 승격, 둘째 앱(admin): 디자인 시스템 배선 범위 밖.
- 단위 테스트 하네스(vitest 등): 이번 단계의 자동 검증은 스토리 스모크·a11y다. 로직 테스트 하네스는 소비자가 생길 때 별도 결정.
- 토큰 값 리브랜딩·next/font 로딩 이동: 값과 폰트 주입은 현행 유지 — 소유권과 체계만 옮긴다.

## 전제 — 토큰 소유권

- [x] **1. 토큰 스타일시트 소유권 이동** — 토큰 정의(팔레트·`@theme` 매핑)가 `apps/web/src/app/globals.css`에 있어 `packages/ui`가 보이지 않는 계약에 의존한다. `packages/ui/src/tokens.css`로 이동하고 앱은 import만 한다(exports에 CSS 서브패스 노출). 값 변경 없이 이동만 — 라이트/다크 시각 동일이 완료 조건.

## 토큰

- [x] **2. 토큰 확장 — 타이포 롤·모션·레이어** — design-system.md 분류표 중 색·radius·shadow·폰트 패밀리만 배선돼 있다. 화면에 실재하는 텍스트 위계만 롤 토큰으로 설계하고(투기 금지), 모션 duration·easing과 레이어(z-index) 단계를 정의한 뒤 전 컴포넌트의 개별 유틸 조합을 치환한다. 스페이싱·브레이크포인트는 프레임워크 기본 스케일 수용 여부를 결정으로 기록한다.
- [x] **3. 토큰 우회 차단 린트 배선** — code-quality.md는 "원시 색·치수 값을 ESLint가 막는다"고 선언하나 배선이 없고, `packages/ui`는 무린트다(typecheck만). 임의 값 구문·비토큰 색 유틸 차단 규칙 + ui 패키지 린트 참여를 배선한다. 문서 선언과 현실의 불일치를 닫는 항목.

## 컴포넌트

- [x] **4. 컴포넌트 인벤토리 감사·승격** — 실증된 중복(버튼 시각의 링크: PostList CTA·페이지네이션)을 승격 기준(도메인 무지 + 둘째 사용처)으로 처리하고, 나머지 패턴(빈 상태 셸·카드 셸·인라인 경고)은 사용처를 세어 기준 충족분만 승격한다. 사용처 하나면 승격 금지 — "승격 없음 + 근거"도 유효한 완료. 승격분은 완료 정의로 자기검증.

## 워크벤치

- [ ] **5. Storybook 배선** — 스토리 0개. `packages/ui`가 설정을 소유하는 Storybook(CSF3)을 배선하고, 프리미티브·승격분의 variant × 상태 × 테마 스토리를 co-locate로 작성, test-runner 스모크 + addon-a11y를 루트 verify 파이프라인에 합류시킨다. 1번이 구조적 전제다(토큰이 앱에 있으면 프리뷰가 스타일 없이 렌더).
