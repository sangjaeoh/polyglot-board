# 백엔드 API base URL(server-only). 개발에서만 기본값 http://localhost:8080, 프로덕션은 필수(누락 시 기동 실패).
BACKEND_API_URL=http://localhost:8080

# 절대 URL 메타데이터(metadataBase)의 base(클라 노출 가능). 미설정 시 http://localhost:3000.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
